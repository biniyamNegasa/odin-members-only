<div id="<%= dom_id post %>">
  <h2>
    <%= post.title %>
  </h2>

  <p>
    <%= post.body %>
  </p>

  <% if user_signed_in? %>
  <p>
    Written by <%= post.user.email %>
  </p>
  <% end %>

</div>
